
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <EditText
        android:id="@+id/edit_text_note_number"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Уникальный номер"
        android:minHeight="48dp" />

    <EditText
        android:id="@+id/edit_text_new_description"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/edit_text_note_number"
        android:layout_marginTop="16dp"
        android:hint="Изменение"
        android:minHeight="48dp" />

    <Button
        android:id="@+id/button_update"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/edit_text_new_description"
        android:layout_marginTop="16dp"
        android:backgroundTint="#F4D950"
        android:text="Update"
        android:textColor="@color/black"
        android:textStyle="bold" />

</RelativeLayout>
